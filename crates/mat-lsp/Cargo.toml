[package]
name = "mat-lsp"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Language server for .song files: diagnostics, completion, hover, definitions and symbols"

[dependencies]
lsp-server = "0.7.9"
lsp-types = "0.94.1"
mat-core = { version = "0.1.0", path = "../mat-core" }
serde_json = "1.0.151"
