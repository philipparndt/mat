[workspace]
resolver = "3"
members = ["crates/mat-core", "crates/mat-cli"]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"

# DSP code is unbearably slow without optimizations.
[profile.dev]
opt-level = 2

[profile.release]
lto = "thin"
codegen-units = 1
